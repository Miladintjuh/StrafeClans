<UserControl x:Class="StrafeLab.ClanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        </Style>
        <Style x:Key="Muted" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}" />
            <Setter Property="FontSize" Value="13" />
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource CardBrush}" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="GridLinesVisibility" Value="None" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="RowBackground" Value="#10172A" />
            <Setter Property="AlternatingRowBackground" Value="#131B30" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserDeleteRows" Value="False" />
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="AutoGenerateColumns" Value="False" />
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#182139" />
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="8" />
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#2B3B63" />
                    <Setter Property="Foreground" Value="#FFFFFF" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="#182139" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="Margin" Value="0,0,4,0" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="18" Background="{StaticResource WindowBgBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Margin="4,0,4,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Rankings" FontSize="34" FontWeight="Bold" />
                <TextBlock x:Name="SubtitleText" Text="Compare public profiles, manage clans, invite friends, and review team stats." Style="{StaticResource Muted}" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Refresh" Click="RefreshButton_Click" Margin="0,0,8,0" />
                <Button Content="Back" Click="BackButton_Click" Background="#29324A" />
            </StackPanel>
        </Grid>

        <TabControl Grid.Row="1" x:Name="RankingsTabs">
            <TabItem Header="Personal">
                <Grid Margin="0,8,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Border Style="{StaticResource Card}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.2*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Public player rankings" FontSize="20" FontWeight="SemiBold" />
                                <TextBlock Text="Only users who manually made their profile public are shown here." Style="{StaticResource Muted}" Margin="0,4,0,10" />
                                <TextBox x:Name="PersonalSearchBox" TextChanged="PersonalSearchBox_TextChanged" ToolTip="Search by username" />
                            </StackPanel>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="12,0,0,0">
                                <Button Content="Search" Click="SearchPersonalButton_Click" Margin="0,0,8,0" />
                                <Button Content="Clear" Click="ClearPersonalSearchButton_Click" Background="#29324A" />
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Border Grid.Row="1" Style="{StaticResource Card}">
                        <DataGrid x:Name="PersonalGrid" MouseDoubleClick="PersonalGrid_MouseDoubleClick">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="1.2*" />
                                <DataGridTextColumn Header="Sessions" Binding="{Binding Sessions}" Width="85" />
                                <DataGridTextColumn Header="Attempts" Binding="{Binding Attempts}" Width="90" />
                                <DataGridTextColumn Header="Clean %" Binding="{Binding CleanRate, StringFormat={}{0:0.0}}" Width="90" />
                                <DataGridTextColumn Header="Moving %" Binding="{Binding MovingRate, StringFormat={}{0:0.0}}" Width="90" />
                                <DataGridTextColumn Header="Counter avg" Binding="{Binding AverageCounterDelayMs, StringFormat={}{0:0.0} ms}" Width="110" />
                                <DataGridTextColumn Header="Click avg" Binding="{Binding AverageClickDelayMs, StringFormat={}{0:0.0} ms}" Width="100" />
                                <DataGridTextColumn Header="Last session" Binding="{Binding LastSessionAt}" Width="150" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <TextBlock x:Name="PersonalStatusText" Grid.Row="2" Style="{StaticResource Muted}" Margin="4,4,0,0" Text="Double-click a public player to open their public profile." />
                </Grid>
            </TabItem>

            <TabItem Header="Clans">
                <Grid Margin="0,8,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Text="Create clan" FontSize="18" FontWeight="SemiBold" />
                                <TextBlock Text="The creator becomes the owner." Style="{StaticResource Muted}" Margin="0,4,0,10" />
                                <TextBox x:Name="ClanNameBox" ToolTip="Clan name" Margin="0,0,0,10" />
                                <Button Content="Create" Click="CreateClanButton_Click" HorizontalAlignment="Left" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="1" Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Text="Invite player" FontSize="18" FontWeight="SemiBold" />
                                <TextBlock Text="Select a clan below, then invite by username." Style="{StaticResource Muted}" Margin="0,4,0,10" />
                                <TextBox x:Name="InviteUsernameBox" ToolTip="Friend username, without @" Margin="0,0,0,10" />
                                <Button Content="Invite" Click="InviteButton_Click" HorizontalAlignment="Left" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="2" Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Text="Sync" FontSize="18" FontWeight="SemiBold" />
                                <TextBlock Text="Upload saved local session summaries so clans can aggregate them." Style="{StaticResource Muted}" TextWrapping="Wrap" Margin="0,4,0,10" />
                                <Button Content="Upload local summaries" Click="UploadButton_Click" HorizontalAlignment="Left" />
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Grid Grid.Row="1" Margin="0,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.9*" />
                            <ColumnDefinition Width="1.35*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1.05*" />
                            <RowDefinition Height="0.75*" />
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource Card}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <TextBlock Text="My clans" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8" />
                                <DataGrid x:Name="ClansGrid" Grid.Row="1" SelectionChanged="ClansGrid_SelectionChanged">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Clan" Binding="{Binding Name}" Width="*" />
                                        <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="85" />
                                        <DataGridTextColumn Header="Members" Binding="{Binding Members}" Width="80" />
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </Border>

                        <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource Card}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <TextBlock Text="Invites" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8" />
                                <DataGrid x:Name="InvitesGrid" Grid.Row="1" MouseDoubleClick="InvitesGrid_MouseDoubleClick">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Clan" Binding="{Binding ClanName}" Width="*" />
                                        <DataGridTextColumn Header="From" Binding="{Binding InvitedByUsername}" Width="110" />
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </Border>

                        <Border Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" Style="{StaticResource Card}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <TextBlock Text="Clan statistics" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8" />
                                <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,10">
                                    <Border Background="{StaticResource SoftBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="10" Margin="0,0,8,0"><StackPanel><TextBlock x:Name="ClanMembersText" Text="0" FontSize="20" FontWeight="Bold" /><TextBlock Text="Members" Style="{StaticResource Muted}" /></StackPanel></Border>
                                    <Border Background="{StaticResource SoftBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="10" Margin="0,0,8,0"><StackPanel><TextBlock x:Name="ClanCleanText" Text="0%" FontSize="20" FontWeight="Bold" /><TextBlock Text="Avg clean" Style="{StaticResource Muted}" /></StackPanel></Border>
                                    <Border Background="{StaticResource SoftBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="10" Margin="0,0,8,0"><StackPanel><TextBlock x:Name="ClanMovingText" Text="0%" FontSize="20" FontWeight="Bold" /><TextBlock Text="Avg moving" Style="{StaticResource Muted}" /></StackPanel></Border>
                                    <Border Background="{StaticResource SoftBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="10"><StackPanel><TextBlock x:Name="ClanMvpText" Text="—" FontSize="20" FontWeight="Bold" /><TextBlock Text="Top clean" Style="{StaticResource Muted}" /></StackPanel></Border>
                                </UniformGrid>
                                <DataGrid x:Name="StatsGrid" Grid.Row="2">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Player" Binding="{Binding Username}" Width="*" />
                                        <DataGridTextColumn Header="Sessions" Binding="{Binding Sessions}" Width="80" />
                                        <DataGridTextColumn Header="Attempts" Binding="{Binding Attempts}" Width="90" />
                                        <DataGridTextColumn Header="Clean %" Binding="{Binding CleanRate, StringFormat={}{0:0}}" Width="80" />
                                        <DataGridTextColumn Header="Moving %" Binding="{Binding MovingRate, StringFormat={}{0:0}}" Width="85" />
                                        <DataGridTextColumn Header="Overlap" Binding="{Binding Overlap}" Width="75" />
                                        <DataGridTextColumn Header="Slow" Binding="{Binding Slow}" Width="70" />
                                        <DataGridTextColumn Header="Counter avg" Binding="{Binding AverageCounterDelayMs, StringFormat={}{0:0.0} ms}" Width="110" />
                                        <DataGridTextColumn Header="Click avg" Binding="{Binding AverageClickDelayMs, StringFormat={}{0:0.0} ms}" Width="100" />
                                        <DataGridTemplateColumn Header="Profile" Width="90">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="View" Click="PlayerProfileButton_Click" Padding="8,4" />
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                                <Border Grid.Row="3" Background="{StaticResource SoftBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="10" Margin="0,10,0,0">
                                    <StackPanel>
                                        <TextBlock Text="Team coaching" FontSize="16" FontWeight="SemiBold" />
                                        <TextBlock x:Name="TeamCoachingText" Style="{StaticResource Muted}" TextWrapping="Wrap" />
                                    </StackPanel>
                                </Border>
                                <TextBlock x:Name="StatusText" Grid.Row="4" Style="{StaticResource Muted}" Margin="0,8,0,0" />
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
